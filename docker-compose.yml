services:
  backend:
    container_name: backend
    build: backend
    ports:
      - 3000:3000
    volumes:
      - ./backend:/usr/src/app

  frontend:
    container_name: frontend
    build: frontend
    ports:
      - 5173:5173
    volumes:
      - ./frontend:/usr/src/app

import { useAuth } from "../components/AuthProvider";

export default function SignIn() {
  const auth = useAuth();
  const handleClick = () => {};
  return (
    <div>
      <h1>Sign In Page</h1>
    </div>
  );
}
